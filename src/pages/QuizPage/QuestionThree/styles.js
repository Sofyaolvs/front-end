import styled from "styled-components"
import { slideAnimation } from "../../../style/global.js"

export const QuestionThreeContainer = styled.div`
  display: flex;
  flex-direction: row;
  gap: 30px;
  animation: ${slideAnimation} 1s;
`
